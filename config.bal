// Configurable FHIR endpoint URL
configurable string fhirEndpointUrl = ?;

// Configurable custom patient data endpoint URL
configurable string customPatientEndpointUrl = ?;

// MLLP listener port
configurable int mllpListenerPort = 2575;

// Management API port for manual replay operations
configurable int managementApiPort = 8080;
